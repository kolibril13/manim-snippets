manim.scene.scene.Scene
=======================

.. currentmodule:: manim.scene.scene

.. autoclass:: Scene

   
   
   .. rubric:: Methods

   .. autosummary::
      :nosignatures:
      :toctree: .
      
        ~Scene.add
        ~Scene.add_foreground_mobject
        ~Scene.add_foreground_mobjects
        ~Scene.add_frames
        ~Scene.add_mobjects_among
        ~Scene.add_mobjects_from_animations
        ~Scene.add_sound
        ~Scene.begin_animations
        ~Scene.bring_to_back
        ~Scene.bring_to_front
        ~Scene.capture_mobjects_in_camera
        ~Scene.clean_up_animations
        ~Scene.clear
        ~Scene.compile_play_args_to_animation_list
        ~Scene.construct
        ~Scene.finish_animations
        ~Scene.force_skipping
        ~Scene.freeze_background
        ~Scene.get_animation_time_progression
        ~Scene.get_attrs
        ~Scene.get_frame
        ~Scene.get_image
        ~Scene.get_mobject_copies
        ~Scene.get_mobject_family_members
        ~Scene.get_mobjects
        ~Scene.get_mobjects_from_last_animation
        ~Scene.get_moving_mobjects
        ~Scene.get_restructured_mobject_list
        ~Scene.get_run_time
        ~Scene.get_time
        ~Scene.get_time_progression
        ~Scene.get_top_level_mobjects
        ~Scene.get_wait_time_progression
        ~Scene.handle_caching_play
        ~Scene.handle_caching_wait
        ~Scene.handle_play_like_call
        ~Scene.idle_stream
        ~Scene.increment_time
        ~Scene.play
        ~Scene.print_end_message
        ~Scene.progress_through_animations
        ~Scene.remove
        ~Scene.remove_foreground_mobject
        ~Scene.remove_foreground_mobjects
        ~Scene.reset_camera
        ~Scene.restructure_mobjects
        ~Scene.revert_to_original_skipping_status
        ~Scene.set_camera
        ~Scene.set_camera_background
        ~Scene.set_camera_pixel_array
        ~Scene.set_variables_as_attrs
        ~Scene.setup
        ~Scene.should_update_mobjects
        ~Scene.show_frame
        ~Scene.tear_down
        ~Scene.update_frame
        ~Scene.update_mobjects
        ~Scene.update_skipping_status
        ~Scene.wait
        ~Scene.wait_until
   
   

   
   
   .. rubric:: Attributes

   .. autosummary::
   
      ~Scene.CONFIG
   
   